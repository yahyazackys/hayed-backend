@extends('frontend.template')

@section('content')
    {{-- <div class="container mx-auto py-16">
        <h1 class="text-4xl font-bold mb-4">{{ $service->servis }}</h1>
        <img src="{{ asset($service->image) }}" class="w-full h-96 object-cover mb-6" alt="{{ $service->servis }}">
        <p class="text-lg text-gray-700">{{ $service->description }}</p>
    </div> --}}
    <div class="w-full h-screen">

    </div>
@endsection
